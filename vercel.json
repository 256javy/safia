{
  "framework": "nextjs",
  "buildCommand": "pnpm generate-manifest && pnpm build",
  "installCommand": "pnpm install --frozen-lockfile",
  "outputDirectory": ".next",
  "env": {
    "NEXTAUTH_URL": "https://safia.app"
  }
}
